pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
rootProject.name = 'service'

include ':rule-engine-lib'
project(":rule-engine-lib").projectDir = new File("../rule-engine-lib")
